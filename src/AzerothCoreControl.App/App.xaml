<Application x:Class="AzerothCoreControl.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:tb="http://www.hardcodet.net/taskbar">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/DarkTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- System-tray icon with a quick-action menu. Keeps supervising while minimized. -->
            <tb:TaskbarIcon x:Key="TrayIcon"
                            ToolTipText="AzerothCore Control"
                            IconSource="/Assets/appicon.ico"
                            DoubleClickCommand="{Binding ShowWindowCommand}"
                            Visibility="Visible">
                <tb:TaskbarIcon.ContextMenu>
                    <!-- Style="{x:Null}" + empty child styles isolate the tray menu from the app theme.
                         Themed implicit menu/scrollbar styles crash the tray ContextMenu because it renders
                         in a separate popup presentation source; default Windows menu chrome is used here. -->
                    <ContextMenu Style="{x:Null}">
                        <ContextMenu.Resources>
                            <Style TargetType="MenuItem" />
                            <Style TargetType="Separator" />
                            <Style TargetType="ScrollBar" />
                        </ContextMenu.Resources>
                        <MenuItem Header="Open window" Command="{Binding ShowWindowCommand}" />
                        <Separator />
                        <MenuItem Header="Start all" Command="{Binding StartAllCommand}" />
                        <MenuItem Header="Stop all" Command="{Binding StopAllCommand}" />
                        <MenuItem Header="Restart world" Command="{Binding RestartWorldCommand}" />
                        <Separator />
                        <MenuItem Header="Quit" Command="{Binding QuitAppCommand}" />
                    </ContextMenu>
                </tb:TaskbarIcon.ContextMenu>
            </tb:TaskbarIcon>
        </ResourceDictionary>
    </Application.Resources>
</Application>
